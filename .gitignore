# Build artifacts (regenerated by `make` / the Printing Press)
/build/
/nhc-pp-cli
/nhc-pp-mcp
*.mcpb

# Transient verification reports
/dogfood-results.json
/workflow-verify-report.json

# Local SQLite cache + OS cruft
*.db
*.sqlite
.DS_Store
